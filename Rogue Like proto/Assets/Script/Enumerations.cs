using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*

public class Enumerations : MonoBehaviour
{
    enum Color {blue, green, red, yellow, purple}; // has to have linear progression . 1, 2, 3 or so
    enum Army {Eldar, Marines, Sisters, Tau, Necrons, Orks};
    enum Model {Warlock, Flamer, Superior, Pathfinder, Boiz};

    // Start is called before the first frame update
    void Start()
    {
        Color paint;
        paint = Color.green;

        print(paint);
        
        Army set;
        set = Army.Tau;
        print(set);

        Model type;
        type = Model.Pathfinder;
        print(type);


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}*/
