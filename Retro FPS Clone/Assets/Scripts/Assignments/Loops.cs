using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Loops : MonoBehaviour
{

    /*public int numEwoks = 20;
    public int ducks = 50;
    public int Legos = 99;
    public int RainDrops = 38;
    public int numCrackers = 12;
    public int bottles = 23;
    public int candies = 87;
    public int shoePairs = 43;
    public int numCups = 5;
    public int rooms = 16;
    
    // Start is called before the first frame update
    void Start()
    {
        for(int i = 0; i <= numEwoks; i++)// will have a finate amount of times it loops, i++ is short hand for i+1, can also be used as i--
        {
            Debug.Log("There are" + i + "Ewoks on a log!");
        }
        for(int i = 99; i >= Legos; i--)
        {
            Debug.Log("There are" + i + "Lego bricks!");
        }
        for(int i = 0; i <= numCrackers; i++)
        {
            Debug.Log("There are" + i + "Crackers!");
        }
        for(int i = 87; i >= candies; i--) 
            Debug.Log("There are" + i + "Candies!");
        }
        for(int i = 0; i <= numCups; i++)
        {
            Debug.Log("There are" + i + "Cups!");
        }



        
        while(ducks >0) // The same is true for counting up, but using <0 rather than >0. [Make sure the Inspector isnt overinding the code.]
        {
            print(ducks);
            print("All the ducks have flown south for winter.");
            ducks --;
        }
        while(RainDrops <38) 
        {
            print(RainDrops);
            print("There are raindrops falling from the sky ");
            RainDrops ++;
        }
        while(bottles >0) 
        {
            print(bottles);
            print("All the bottles have broken.");
            bottles --;
        }
        while(shoePairs <43) 
        {
            print(shoePairs);
            print("The Shoes keep building.");
            shoePairs ++;
        }
        while(rooms >0)
        {
            print(rooms);
            print("There are less and less rooms in each house.");
            rooms --;
        }*/
    }

   